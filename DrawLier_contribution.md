### **프로젝트 기여 내용 요약 (dhtmd님)**

이 문서는 DrawLier 프로젝트에서 제가 기여한 주요 기능 구현 및 코드 개선 사항을 정리한 것입니다.

### **1. UI/UX 개선 및 코드 리팩토링**

#### **1.1. 공통 버튼 효과 적용 및 UI 유틸리티 클래스 생성**

-   **목표:** 여러 UI 클래스에 중복으로 존재하던 버튼 효과 코드를 하나로 통합하여 코드의 재사용성을 높이고 유지보수를 용이하게 합니다.
-   **구현 내용:**
    1.  `UIUtils.java` 클래스를 신규 생성했습니다.
    2.  기존 `VotingUI`, `ResultUI` 등에 흩어져 있던 버튼 효과(마우스 호버, 클릭 시 이미지 밝기 조절) 코드를 `UIUtils.applyButtonEffects(JButton button)` 라는 `public static` 메소드로 분리하여 이전했습니다.
    3.  `VotingUI`, `ResultUI`, `InsertAnswerUI`, `RoomListUI`, `JavaChatClientView` 등 프로젝트 내 모든 이미지 버튼 생성 부분에서 `UIUtils.applyButtonEffects()`를 호출하도록 코드를 수정하여 중복을 제거했습니다.

-   **기능 흐름:**
    1.  각 UI 클래스에서 이미지 아이콘이 있는 `JButton`을 생성합니다.
    2.  `UIUtils.applyButtonEffects(해당_버튼);` 메소드를 호출합니다.
    3.  `applyButtonEffects` 메소드는 전달받은 버튼의 아이콘 이미지를 기반으로, 마우스를 올렸을 때(밝게)와 클릭했을 때(어둡게) 사용할 `ImageIcon`을 각각 생성하여 `setRolloverIcon`, `setPressedIcon`으로 등록합니다.
    4.  이를 통해 모든 이미지 버튼이 일관된 시각적 피드백을 제공하게 됩니다.

#### **1.2. 이미지 기반 이모티콘 시스템 구현**

-   **목표:** 기존에 하드코딩된 텍스트 이모티콘을 `resources/Imoji` 폴더의 이미지 파일을 동적으로 로드하여 사용하는 방식으로 개선하여, 확장성과 시각적 풍부함을 높입니다.
-   **구현 내용:**
    1.  `JavaChatClientView.java`에 `Map<String, ImageIcon> emojiMap` 필드를 추가했습니다.
    2.  `loadEmojis()` 메소드를 새로 구현하여, 프로그램 시작 시 `/Imoji` 폴더의 모든 이미지 파일을 읽어 `emojiMap`에 저장하도록 했습니다. (파일 이름이 이모티콘의 키가 됨)
    3.  `showEmojiPicker()` 메소드를 수정하여, `emojiMap`의 이미지 아이콘들을 `JButton`에 담아 선택창을 동적으로 생성하도록 변경했습니다.
    4.  이모티콘 버튼 클릭 시, 일반 텍스트 대신 `/emoji [이미지_파일명]` 형태의 특별 프로토콜을 서버로 전송하도록 수정했습니다.
    5.  `appendChatMessage()` 메소드를 수정하여, 서버로부터 `/emoji` 프로토콜이 포함된 메시지를 수신하면, `emojiMap`에서 해당 이미지를 찾아 텍스트 대신 이미지 `JLabel`을 채팅창에 표시하도록 구현했습니다.

-   **기능 흐름:**
    1.  `JavaChatClientView`가 생성될 때 `loadEmojis()`가 호출되어 모든 이모티콘 이미지를 메모리에 로드합니다.
    2.  사용자가 이모티콘 버튼을 클릭하면 `showEmojiPicker()`가 `emojiMap`의 이미지들로 선택창을 보여줍니다.
    3.  사용자가 특정 이모티콘을 클릭하면, 클라이언트는 서버로 `[사용자명]: /emoji [선택한_이모티콘_키]` 메시지를 전송합니다.
    4.  서버는 이 메시지를 해당 방의 모든 클라이언트에게 그대로 전달합니다.
    5.  메시지를 수신한 각 클라이언트의 `appendChatMessage()` 메소드는 `/emoji` 키워드를 감지하고, 뒤따라오는 키 값을 이용해 `emojiMap`에서 `ImageIcon`을 찾아 채팅 말풍선 안에 이미지를 렌더링합니다.

#### **1.3. 채팅 UI 개선 (말풍선 정렬 문제 해결)**

-   **목표:** 긴 텍스트 메시지를 전송할 때, 내가 보낸 메시지 말풍선이 오른쪽 끝에 제대로 정렬되지 않고 중간에 어중간하게 위치하는 레이아웃 문제를 해결합니다.
-   **구현 내용:**
    1.  `JavaChatClientView.java`의 `appendChatMessage()` 메소드 내부를 수정했습니다.
    2.  텍스트(`JTextArea`)를 담는 말풍선 패널(`RoundedBubblePanel`)에 `setMaximumSize()`를 사용하여 최대 너비를 약 250px로 고정했습니다.

-   **기능 흐름:**
    1.  사용자가 긴 텍스트 메시지를 입력합니다.
    2.  `appendChatMessage` 메소드는 말풍선 패널을 생성할 때, 이 패널의 최대 너비를 고정값으로 설정합니다.
    3.  `JTextArea`는 자신이 포함된 부모 패널(말풍선)의 최대 너비를 넘지 않는 선에서 자동으로 줄바꿈을 수행합니다.
    4.  결과적으로 말풍선 전체의 크기가 예측 가능하게 제한되어, `FlowLayout.RIGHT` 설정에 따라 화면 오른쪽에 명확하게 정렬됩니다.

### **2. 핵심 게임 로직 구현 및 안정성 강화**

#### **2.1. 방 나가기 및 플레이어 퇴장 처리**

-   **목표:** 사용자가 게임 방에서 나갈 때, 해당 정보가 서버와 다른 클라이언트들에게 정상적으로 전파되도록 합니다.
-   **구현 내용:**
    1.  **`JavaChatClientView.java`**: '나가기' 버튼에 `leaveRoom()` 메소드를 연결하여, 버튼 클릭 시 서버로 `/leaveRoom` 프로토콜을 전송하도록 구현했습니다.
    2.  **`DrawServer.java`**: `/leaveRoom` 프로토콜을 수신하면, `handleLeaveRoom()` 메소드를 호출하여 해당 유저를 `GameRoom`에서 제거하고, 방에 남아있는 다른 유저들에게 `/playerLeft [나간유저]` 프로토콜을 전송하도록 구현했습니다.
    3.  **`JavaChatClientView.java`**: `/playerLeft` 프로토콜을 수신하면, 채팅창에 퇴장 메시지를 출력하고 `updatePlayerList()`를 호출하여 플레이어 목록 UI를 갱신하도록 구현했습니다.

-   **기능 흐름:**
    1.  사용자가 '나가기' 버튼을 클릭하면, 클라이언트는 서버로 `/leaveRoom` 메시지를 보냅니다.
    2.  서버는 해당 유저를 방에서 제거하고, 같은 방의 다른 모든 유저에게 ` /playerLeft [나간유저]` 메시지를 전파합니다.
    3.  메시지를 받은 클라이언트들은 채팅창에 "[퇴장] OOO님이 퇴장했습니다." 메시지를 출력하고, 화면 우측의 플레이어 목록에서 해당 유저를 제거합니다.

#### **2.2. 방장 자동 위임 기능 구현**

-   **목표:** 게임 대기 중에 방장이 방을 나가더라도 방이 사라지지 않고, 다음 순서의 플레이어에게 방장 권한이 자동으로 위임되도록 하여 게임의 연속성과 안정성을 높입니다.
-   **구현 내용:**
    1.  **`DrawServer.java`**:
        -   `handleLeaveRoom()` 메소드 내부에, 나가는 플레이어가 방장인지 확인하는 로직을 추가했습니다.
        -   방장이 맞고 남은 인원이 있다면, `GameRoom`의 플레이어 목록에서 다음 순서(0번 인덱스)의 플레이어를 새로운 방장으로 지정하고, `/hostChanged [새방장이름]` 프로토콜을 방에 있는 모든 클라이언트에게 전송하도록 구현했습니다.
    2.  **`GameRoom.java`**:
        -   서버에서 새로운 방장 이름을 설정할 수 있도록 `setHostName(String hostName)` 메소드를 추가했습니다.
    3.  **`JavaChatClientView.java`**:
        -   서버로부터 `/hostChanged` 프로토콜을 수신하는 로직을 `ListenNetwork` 스레드에 추가했습니다.
        -   해당 프로토콜을 받으면, 상단 UI의 방장 이름 라벨(`lblHostName`)과 플레이어 목록의 왕관 아이콘을 업데이트하고, '게임 시작' 버튼의 활성화 상태를 동적으로 변경하도록 구현했습니다.

-   **기능 흐름:**
    1.  방장이 방을 나가면, 서버는 `handleLeaveRoom()`에서 이를 감지하고 다음 플레이어를 새 방장으로 지정합니다.
    2.  서버는 해당 방의 모든 클라이언트에게 `/hostChanged [새방장이름]` 메시지를 전송합니다.
    3.  메시지를 받은 클라이언트들은 새 방장 정보를 UI에 즉시 반영합니다. (왕관 아이콘 이동, 방장 이름 변경 등)
    4.  새로 방장이 된 클라이언트는 '게임 시작' 버튼이 활성화됩니다.

#### **2.3. 화면 전환 시 연결 유지 (버그 수정)**

-   **목표:** 게임 방에서 로비로 나가거나, 게임 종료 후 결과 창에서 로비로 이동할 때 "로그인 실패" 오류와 함께 프로그램이 비정상 종료되는 치명적인 버그를 수정합니다.
-   **구현 내용:**
    1.  **`RoomListUI.java`**:
        -   기존 연결(Socket, Streams)을 재사용하는 새로운 생성자를 추가했습니다.
    2.  **`JavaChatClientView.java`, `ResultUI.java` 등**:
        -   게임 방이나 결과 창에서 로비로 돌아갈 때, 새로운 소켓 연결을 생성하는 대신, 기존에 사용하던 `socket`, `dis`, `dos` 객체를 `RoomListUI`의 새 생성자로 그대로 전달하도록 수정했습니다.

-   **기능 흐름:**
    1.  사용자가 '나가기' 또는 '로비로 이동' 버튼을 클릭합니다.
    2.  현재 화면은 서버에 `/leaveRoom` 메시지를 보낸 후, **새로운 연결을 만들지 않고** 기존 연결 객체들을 다음 화면(`RoomListUI`)에 넘겨줍니다.
    3.  `RoomListUI`는 전달받은 연결을 사용하여 서버와 통신을 이어갑니다.
    4.  이로써 불필요한 재로그인 시도가 사라지고, 단일 연결이 유지된 상태로 안정적인 화면 전환이 이루어집니다.

### **3. 그리기 도구 기능 구현**

-   **목표:** 사용자가 그림을 그릴 때 색상, 굵기, 지우개 기능을 사용하여 더 다채로운 표현을 할 수 있도록 합니다.
-   **구현 내용:**
    1.  **`JavaChatClientView.java`**:
        -   `createToolPanel()` 메소드 내에 `JColorChooser`를 여는 '색상 선택' 버튼, 펜 굵기를 선택하는 `JComboBox`, 그림판 배경색으로 색상을 변경하는 '지우개' 버튼을 추가했습니다.
        -   각 컨트롤에서 값이 변경될 때마다 `currentColor`, `strokeWidth` 필드 값이 업데이트되도록 구현했습니다.
    2.  **`DrawingPanel` (내부 클래스)**:
        -   `mouseDragged` 이벤트가 발생할 때, `currentColor`와 `strokeWidth` 필드 값을 참조하여 선을 그리도록 수정했습니다.
        -   그리기 정보(좌표, 색상, 굵기)를 `/draw [좌표] [색상] [굵기]` 형태의 프로토콜로 만들어 서버에 전송하도록 구현했습니다.
    3.  **`DrawServer.java`**:
        -   클라이언트로부터 `/draw` 프로토콜을 수신하면, 해당 방의 다른 모든 클라이언트에게 메시지를 그대로 전달(Broadcast)하는 로직을 추가했습니다.
    4.  **`DrawingPanel` (내부 클래스)**:
        -   서버로부터 `/draw` 프로토콜을 수신하면, `processDrawCommand` 메소드가 메시지를 파싱하여 전달받은 색상과 굵기 값으로 다른 사람의 그림을 동일하게 그려주도록 구현했습니다.

-   **기능 흐름:**
    1.  '나'의 턴에 사용자가 '색상'이나 '굵기'를 변경하면, `JavaChatClientView`의 `currentColor`, `strokeWidth` 변수 값이 변경됩니다.
    2.  사용자가 그림을 그리면, `DrawingPanel`은 현재 설정된 `currentColor`, `strokeWidth` 값으로 선을 그리고, 이 정보를 `/draw` 프로토콜에 담아 서버로 전송합니다.
    3.  서버는 이 메시지를 다른 플레이어들에게 전달합니다.
    4.  다른 플레이어들의 `DrawingPanel`은 수신한 `/draw` 프로토콜의 색상과 굵기 값으로 '나'의 그림을 똑같이 재현합니다. '지우개'는 사실상 배경색과 동일한 색으로 덧칠하는 방식으로 구현되었습니다.
