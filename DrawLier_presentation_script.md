### **DrawLier 프로젝트 기여 내용 발표 대본**

**(0:00-0:30) 오프닝**

안녕하세요, DrawLier 프로젝트에서 개발을 맡은 [본인 이름]입니다. 지금부터 제가 프로젝트에 기여한 주요 기능과 코드 개선 사항에 대해 발표를 시작하겠습니다.

저는 크게 **UI/UX 개선**, **핵심 로직 구현 및 안정성 강화**, 그리고 **주요 게임 플레이 기능 구현** 세 가지 파트에 집중하여 작업했습니다. 각 파트별로 어떤 문제를 해결하고 어떻게 구현했는지 코드와 함께 설명드리겠습니다.

---

**(0:30-3:00) 파트 1: UI/UX 개선 및 코드 리팩토링**

첫 번째 파트는 사용자 경험을 개선하고 코드의 효율성을 높인 내용입니다.

#### **(0:30) 1. 공통 버튼 효과 적용**

`[화면: 수정 전 VotingUI.java와 수정 후 UIUtils.java를 나란히 보여주세요.]`

먼저, 프로젝트 전반의 UI 일관성을 높이고 코드 중복을 제거하기 위해 공통 버튼 효과 기능을 구현했습니다.

기존에는 보시는 것처럼 `VotingUI`, `ResultUI` 등 여러 클래스에 마우스 호버 시 이미지가 밝아지고, 클릭 시 어두워지는 효과 코드가 각각 중복으로 작성되어 있었습니다.

이 문제를 해결하기 위해, 저는 `UIUtils`라는 공용 클래스를 만들고, `applyButtonEffects`라는 static 메소드를 구현했습니다. 이 메소드는 `RescaleOp` 필터를 사용해 원본 이미지의 밝기를 조절하여 'Rollover'와 'Pressed' 아이콘을 동적으로 생성하고, 버튼에 적용합니다.

이제 각 UI 클래스에서는 이 `UIUtils.applyButtonEffects` 메소드를 한 줄만 호출하면 되므로, 코드의 재사용성과 유지보수성이 크게 향상되었습니다.

#### **(1:30) 2. 이미지 기반 이모티콘 시스템**

`[화면: JavaChatClientView.java의 loadEmojis(), showEmojiPicker(), appendChatMessage() 메소드를 차례로 보여주세요.]`

다음은 채팅 경험을 풍부하게 만들기 위해 구현한 이미지 이모티콘 시스템입니다.

기존의 하드코딩된 텍스트 이모티콘 방식은 확장성이 떨어졌습니다. 저는 이를 개선하기 위해, `resources/Imoji` 폴더의 이미지 파일을 동적으로 로드하는 방식으로 변경했습니다.

`JavaChatClientView`가 생성될 때 `loadEmojis` 메소드가 호출되어, 보시는 것처럼 `/Imoji` 폴더의 모든 이미지 파일을 읽어 `emojiMap`이라는 `HashMap`에 저장합니다.

사용자가 이모티콘 버튼을 누르면, `showEmojiPicker` 메소드가 이 `emojiMap`을 기반으로 이미지 버튼들을 동적으로 생성하여 선택창을 보여줍니다.

사용자가 이모티콘을 선택하면, 클라이언트는 `/emoji`라는 특별한 프로토콜과 파일명을 서버로 전송합니다. 서버는 이 메시지를 그대로 다른 클라이언트들에게 전달하고, 메시지를 수신한 클라이언트의 `appendChatMessage` 메소드는 `/emoji` 프로토콜을 감지하여, `emojiMap`에서 해당 이미지를 찾아 채팅창에 텍스트 대신 이미지로 표시합니다.

#### **(2:30) 3. 채팅 UI 정렬 문제 해결**

`[화면: JavaChatClientView.java의 appendChatMessage() 메소드에서 bubble.setMaximumSize() 부분을 강조해주세요.]`

마지막 UI 개선 사항은 채팅 정렬 버그 수정입니다. 긴 텍스트를 입력했을 때 말풍선이 오른쪽에 제대로 붙지 않는 문제가 있었습니다.

원인은 `JTextArea`의 크기가 유동적이어서 레이아웃이 깨지는 것이었습니다. 저는 `appendChatMessage` 메소드에서 말풍선 역할을 하는 패널에 `setMaximumSize`를 설정하여 최대 너비를 고정했습니다.

이 간단한 조치를 통해, 텍스트가 길어지면 `JTextArea`가 이 최대 너비 안에서 자동으로 줄바꿈을 하게 되어, 보시는 것처럼 말풍선이 항상 오른쪽에 깔끔하게 정렬되도록 문제를 해결했습니다.

---

**(3:00-7:30) 파트 2: 핵심 게임 로직 구현 및 안정성 강화**

두 번째 파트는 게임의 안정성과 핵심 로직에 관련된 내용입니다.

#### **(3:00) 1. 방장 자동 위임 기능**

`[화면: DrawServer.java의 handleLeaveRoom() 메소드와 JavaChatClientView.java의 /hostChanged 처리 부분을 보여주세요.]`

가장 중요한 기능 중 하나인 방장 자동 위임 기능입니다. 방장이 갑자기 나갔을 때 게임이 중단되는 것을 막기 위해 구현했습니다.

먼저 서버의 `DrawServer.java`를 보시면, `handleLeaveRoom`이라는 메소드를 구현했습니다. 클라이언트의 연결이 끊어지면 이 메소드가 호출되는데, 여기서 나간 사람이 방장인지 확인합니다.

만약 방장이 맞고 방에 남은 인원이 있다면, `GameRoom`의 플레이어 목록에서 다음 순서의 플레이어를 새로운 방장으로 지정합니다. 그리고 `/hostChanged`라는 프로토콜과 함께 새 방장의 이름을 방에 있는 모든 클라이언트에게 전송합니다.

클라이언트 측인 `JavaChatClientView`에서는 이 `/hostChanged` 프로토콜을 수신하면, 자신이 새로운 방장인지 확인하고, `isHost` 상태를 변경합니다. 그리고 보시는 것처럼 플레이어 목록의 왕관 아이콘을 새로운 방장에게 옮기고, '게임 시작' 버튼을 활성화하는 등 UI를 동적으로 갱신합니다.

#### **(4:30) 2. 화면 전환 시 연결 유지 버그 수정**

`[화면: RoomListUI.java의 두 생성자와, JavaChatClientView.java의 leaveRoom() 메소드를 보여주세요.]`

다음은 제가 해결한 치명적인 버그입니다. 이전에는 게임 방에서 로비로 나갈 때 "로그인 실패" 오류가 발생하며 프로그램이 종료되었습니다.

원인은 화면을 전환할 때마다 새로운 `Socket` 연결을 시도하여 서버와 충돌이 발생했기 때문입니다.

저는 이 문제를 해결하기 위해, `RoomListUI`에 기존 연결 정보를 그대로 인자로 받는 새로운 생성자를 추가했습니다.

그리고 `JavaChatClientView`의 `leaveRoom` 메소드나, `ResultUI`에서 '로비로 이동' 버튼을 누를 때, 새로운 `RoomListUI`를 생성하는 대신, 보시는 것처럼 **기존에 사용하던 `socket`과 스트림 객체들을 이 새 생성자로 전달**하도록 수정했습니다.

이로써 불필요한 재연결 시도가 사라지고, 단일 연결이 계속 유지되어 안정적인 화면 전환이 가능해졌습니다.

#### **(5:30) 3. 투표 및 게임 결과 처리 흐름 구현**

`[화면: VotingUI.java의 ListenVoteResult 스레드와 InsertAnswerUI.java, ResultUI.java를 차례로 보여주세요.]`

게임의 핵심 흐름인 투표와 결과 처리 로직을 구현했습니다.

`VotingUI`에서는 투표가 끝나면 서버로부터 `/voteResult` 프로토콜을 받습니다. 여기서 라이어가 지목되었는지 여부에 따라 흐름이 나뉩니다.

-   **라이어가 지목된 경우:** 지목된 라이어에게는 제가 구현한 `InsertAnswerUI`가 열려 정답을 입력할 마지막 기회를 줍니다.
-   **시민이 지목된 경우:** 라이어의 승리로 게임이 즉시 종료되며, 제가 구현한 `ResultUI`가 열려 최종 결과를 표시합니다.

라이어가 `InsertAnswerUI`에서 정답을 제출하면, 서버는 정답 여부를 판단하여 `/finalResult` 프로토콜로 최종 승패를 알리고, 클라이언트는 이 정보를 바탕으로 `ResultUI`를 통해 사용자에게 명확한 게임 결과를 보여줍니다.

#### **(6:30) 4. 게임 종료 방 자동 삭제**

`[화면: DrawServer.java의 setRoomStatus()와 getFilteredRoomList() 메소드를 보여주세요.]`

사용자가 이미 끝난 게임에 참여하는 것을 막기 위해, 게임이 종료된 방은 목록에서 자동으로 사라지도록 구현했습니다.

서버의 `DrawServer.java`를 보시면, 게임이 최종적으로 끝나면 해당 방의 상태를 `FINISH`로 설정하는 `setRoomStatus` 메소드를 호출합니다.

그리고 클라이언트가 방 목록을 요청할 때마다, `getFilteredRoomList` 메소드가 `FINISH` 상태의 방을 제외한 목록만 필터링하여 전송합니다. 이를 통해 로비에서는 항상 참여 가능한 방만 보이게 됩니다.

---

**(7:30-9:00) 파트 3: 그리기 도구 기능 구현**

마지막으로, 게임의 핵심 재미 요소인 그리기 도구 기능입니다.

`[화면: JavaChatClientView.java의 createToolPanel()과 DrawingPanel 내부 클래스의 mouseDragged(), processDrawCommand()를 보여주세요.]`

사용자가 더 다채롭게 그림을 그릴 수 있도록 색상 변경, 굵기 조절, 지우개 기능을 추가했습니다.

`createToolPanel` 메소드에서 '색상 선택', '굵기', '지우개' UI 컴포넌트를 추가했습니다. 사용자가 여기서 값을 변경하면 `currentColor`와 `strokeWidth` 변수에 즉시 반영됩니다.

`DrawingPanel`의 `mouseDragged` 메소드에서는, 그림을 그릴 때 이 변수 값을 참조하여 선을 그리고, 동시에 좌표, 색상, 굵기 정보를 `/draw` 프로토콜에 담아 서버로 전송합니다.

서버는 이 메시지를 받아서 다른 모든 클라이언트에게 전달하고, 다른 클라이언트들은 `processDrawCommand` 메소드를 통해 이 정보를 파싱하여 동일한 위치에, 동일한 색상과 굵기로 그림을 재현합니다. 지우개 기능은 사실상 배경색과 같은 색으로 덧칠하는 방식으로 구현했습니다.

---

**(9:00-9:30) 클로징**

이상으로 제가 DrawLier 프로젝트에서 구현하고 개선한 내용에 대한 발표를 마치겠습니다. UI/UX 개선부터 핵심 로직 구현, 그리고 안정성 강화까지 프로젝트의 완성도를 높이는 데 기여하고자 노력했습니다.

들어주셔서 감사합니다.
