### **1. 소스 파일 실행 순서 (상세)**

1.  **서버 실행**
    *   **파일:** `org.example.DrawServer.java`
    *   **동작:**
        1.  `main` 메소드가 실행되면 `DrawServer` 객체가 생성됩니다.
        2.  서버는 지정된 포트(예: 7777)를 열고 클라이언트의 접속을 무한 대기합니다 (`serverSocket.accept()`).
        3.  클라이언트가 접속하면, 해당 클라이언트와의 통신을 전담할 별도의 스레드(`ClientHandler`)를 생성하고 시작합니다.
        4.  서버는 다시 다음 클라이언트의 접속을 대기합니다.

2.  **클라이언트 실행 및 서버 접속**
    *   **파일:** `org.example.LiarGameClientLoginUI.java`
    *   **동작:**
        1.  `main` 메소드가 실행되어 로그인 UI가 화면에 나타납니다.
        2.  사용자가 닉네임을 입력하고 '접속' 버튼을 클릭합니다.
        3.  클라이언트는 `DrawServer`의 IP 주소와 포트로 소켓 연결을 시도합니다.
        4.  연결 성공 시, 로그인 UI는 사라지고 `RoomListUI`(게임 로비)를 생성하여 화면에 표시합니다. 이 과정에서 서버에 닉네임 정보를 전송합니다.

3.  **게임 로비 (방 생성 또는 참여)**
    *   **파일:** `org.example.RoomListUI.java`
    *   **동작:**
        1.  로비 UI는 서버로부터 현재 생성된 게임 방 목록 정보를 받아 화면에 표시합니다.
        2.  **방 만들기:** 사용자가 '방 만들기' 버튼을 클릭하면, 서버에 방 생성 요청을 보냅니다. 서버는 새로운 `GameRoom`을 만들고, 요청한 클라이언트는 해당 방으로 바로 입장합니다.
        3.  **방 참여:** 사용자가 목록에서 특정 방을 선택하고 '참여' 버튼을 클릭하면, 서버에 해당 방의 입장 요청을 보냅니다. 서버는 `GameRoom`에 해당 클라이언트를 추가합니다.
        4.  방에 입장하면 로비 UI는 사라지고 `JavaChatClientView`(게임 화면)가 나타납니다.

4.  **게임 진행**
    *   **파일:** `JavaChatClientView.java`, `GameRoom.java` (서버 측)
    *   **동작:**
        1.  **게임 시작:** 방장이 '게임 시작' 버튼을 누르면, 클라이언트는 서버에 게임 시작 요청을 보냅니다.
        2.  `GameRoom`은 카테고리에 맞는 주제어와 라이어를 선정하고, 각 클라이언트에게 역할을 부여하며 게임 상태(주제어, 순서 등)를 전송합니다.
        3.  **그리기/채팅:** 플레이어는 자신의 턴에 그림을 그립니다. 마우스 좌표 정보가 실시간으로 서버에 전송되고, 서버는 이 정보를 같은 방의 다른 클라이언트들에게 중계하여 모든 플레이어의 화면에 그림이 동일하게 그려집니다. 채팅 역시 동일한 방식으로 중계됩니다.
        4.  **정답 제출:** 플레이어가 정답을 입력하면(`InsertAnswerUI`), 서버로 전송됩니다. 서버는 정답 여부를 판별하여 결과를 해당 클라이언트에게 다시 알려줍니다.

5.  **투표 및 결과 확인**
    *   **파일:** `VotingUI.java`, `ResultUI.java`
    *   **동작:**
        1.  게임 시간이 종료되거나 조건이 충족되면, 서버는 모든 클라이언트에게 투표 시작을 알립니다.
        2.  각 클라이언트 화면에 `VotingUI`가 나타나고, 라이어로 의심되는 사람을 투표하여 서버에 전송합니다.
        3.  서버(`GameRoom`)는 투표 결과를 집계하여 라이어가 누구인지, 승리 팀은 어디인지 판별합니다.
        4.  서버가 모든 클라이언트에게 최종 결과를 전송하면, 각 클라이언트 화면에 `ResultUI`가 나타나 결과를 보여줍니다.

---

### **2. 구현된 기능 (사용자 정의 프로토콜 중심)**

클라이언트와 서버는 `헤더#데이터` 형식의 간단한 텍스트 기반 프로토콜을 통해 통신합니다.

| 기능 | 프로토콜 (Client → Server) | 프로토콜 (Server → Client) | 설명 |
| :--- | :--- | :--- | :--- |
| **로그인** | `LOGIN#[닉네임]` | `LOGIN_SUCCESS` 또는 `LOGIN_FAIL` | 클라이언트가 자신의 닉네임을 서버에 알리고 접속을 요청합니다. |
| **채팅 메시지** | `CHAT#[메시지 내용]` | `CHAT#[닉네임]: [메시지 내용]` | 사용자가 입력한 채팅을 서버로 보내면, 서버가 해당 방의 모든 유저에게 닉네임을 붙여 전달합니다. |
| **방 목록 요청** | `ROOMLIST_REQUEST` | `ROOMLIST#[방제목1],[방제목2],...` | 클라이언트가 로비에 입장 시 방 목록을 요청하고, 서버는 현재 생성된 방 목록을 보내줍니다. |
| **방 생성** | `CREATE_ROOM#[방제목]` | `CREATE_SUCCESS#[방제목]` | 클라이언트가 방 생성을 요청하면 서버가 방을 만들고 성공 여부와 방 정보를 클라이언트에게 전달합니다. |
| **방 참여** | `JOIN_ROOM#[방제목]` | `JOIN_SUCCESS#[방제목]` | 클라이언트가 특정 방 참여를 요청하면, 서버는 해당 방에 입장시키고 성공 여부를 알립니다. |
| **게임 시작** | `GAME_START` | `GAME_START#[주제어]`, `LIAR_GAME_START#[라이어 주제어]` | 방장이 게임 시작을 요청하면, 서버는 일반 플레이어와 라이어에게 각각 다른 주제어를 보내며 게임 시작을 알립니다. |
| **그림 그리기** | `DRAW#[x],[y]` <br> `MOUSEDOWN#[x],[y]` <br> `MOUSEUP` | `DRAW#[x],[y]` <br> `MOUSEDOWN#[x],[y]` <br> `MOUSEUP` | 클라이언트의 마우스 움직임(좌표, 누름, 뗌)을 서버로 보내면, 서버가 이를 다른 클라이언트들에게 그대로 중계합니다. |
| **정답 제출** | `ANSWER#[제출한 정답]` | `ANSWER_CORRECT` 또는 `ANSWER_WRONG` | 클라이언트가 정답을 제출하면 서버가 정답 여부를 판별하여 결과를 회신합니다. |
| **라이어 투표** | `VOTE#[지목한 닉네임]` | `VOTE_RESULT#[결과 내용]` | 클라이언트가 투표한 닉네임을 서버로 보내면, 서버는 결과를 집계하여 최종 결과를 모든 클라이언트에게 알립니다. |
| **플레이어 목록 갱신** | (방 입장/퇴장 시 자동) | `PLAYER_LIST#[닉네임1],[닉네임2],...` | 새로운 유저가 방에 들어오거나 나갈 때마다, 서버가 현재 방의 최신 플레이어 목록을 모든 클라이언트에게 전송합니다. |
